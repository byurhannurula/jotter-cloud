version: 2
updates:
  # npm/pnpm dependencies. Grouped so routine minor/patch bumps arrive as one PR.
  - package-ecosystem: npm
    directory: /
    # Open PRs against dev; promote to main via the normal dev -> main PR.
    target-branch: dev
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      minor-and-patch:
        update-types:
          - minor
          - patch

  # Keep the GitHub Actions used in CI current.
  - package-ecosystem: github-actions
    directory: /
    # Open PRs against dev; promote to main via the normal dev -> main PR.
    target-branch: dev
    schedule:
      interval: weekly
    groups:
      actions:
        update-types:
          - minor
          - patch
